+ implement scraping pipeline for noaa (insitu + flask) data
+ include validation scheme using pandera
+ include global configuration module
+ include unit and regression tests
+ include new workflow "ci-test-download.yaml" that triggers test for downloading data only manually (dispatch) or via specific commit message "test download data".
